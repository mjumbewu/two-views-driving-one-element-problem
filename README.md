# Dual View Element Pitfalls

Simple example of driving two elements with the same view code. To run it just do:

```bash
python3 -m http.server 8001
```

Then open [http://localhost:8001](http://localhost:8001) in your browser.
